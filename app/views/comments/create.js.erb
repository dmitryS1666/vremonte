<% if @comment.valid? %>
  <% if @commentable.is_a? Request %>
    $('.comments-list').append(
        '<%= j render partial: @comment, locals: { comment: @comment} %>'
    );
    $('.request input#comment_body').val('');
  <% end %>
<% end %>